name: Build, Push, and Deploy Docker Image (SAND 测试)

on:
  push:
    branches:
      - sand
  workflow_dispatch:

jobs:
  docker-build-and-push:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    env:
      ACR_NAME: ${{ vars.ACR_NAME }}
      ACR_LOGIN_SERVER: ${{ vars.ACR_LOGIN_SERVER }}
      IMAGE_NAME: tools-for-the-tough-days
      AZURE_CLIENT_ID: ${{ secrets.AZUREAPPSERVICE_CLIENTID_DOCKER }}
      AZURE_TENANT_ID: ${{ vars.AZURE_TENANTID }}
      AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTIONID }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate Azure OIDC config exists
        shell: bash
        run: |
          if [ -z "${AZURE_CLIENT_ID}" ] || [ -z "${AZURE_TENANT_ID}" ] || [ -z "${AZURE_SUBSCRIPTION_ID}" ] || [ -z "${ACR_NAME}" ] || [ -z "${ACR_LOGIN_SERVER}" ]; then
            echo "Missing one or more required values:"
            echo "- AZUREAPPSERVICE_CLIENTID_DOCKER"
            echo "- AZURE_TENANTID (repository variable)"
            echo "- AZURE_SUBSCRIPTIONID (repository variable)"
            echo "- ACR_NAME (repository variable)"
            echo "- ACR_LOGIN_SERVER (repository variable)"
            exit 1
          fi

      - name: Login to Azure (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ env.AZURE_CLIENT_ID }}
          tenant-id: ${{ env.AZURE_TENANT_ID }}
          subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

      - name: Login to Azure Container Registry (OIDC test)
        shell: bash
        run: |
          az acr login --name "$ACR_NAME"

      - name: Build Docker image with sand tag
        run: |
          docker build \
            -t $ACR_LOGIN_SERVER/$IMAGE_NAME:sand \
            .
      - name: Push Docker image sand tag
        run: |
          docker push $ACR_LOGIN_SERVER/$IMAGE_NAME:sand
  deploy-to-webapp:
    runs-on: ubuntu-latest
    needs: docker-build-and-push
    permissions:
      id-token: write
      contents: read

    env:
      ACR_LOGIN_SERVER: ${{ vars.ACR_LOGIN_SERVER }}
      IMAGE_NAME: tools-for-the-tough-days
      AZURE_CLIENT_ID: ${{ secrets.AZUREAPPSERVICE_CLIENTID_DOCKER }}
      AZURE_TENANT_ID: ${{ vars.AZURE_TENANTID }}
      AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTIONID }}

    steps:
      - name: Validate Azure OIDC config exists
        shell: bash
        run: |
          if [ -z "${AZURE_CLIENT_ID}" ] || [ -z "${AZURE_TENANT_ID}" ] || [ -z "${AZURE_SUBSCRIPTION_ID}" ] || [ -z "${ACR_LOGIN_SERVER}" ]; then
            echo "Missing one or more Azure OIDC config values:"
            echo "- AZUREAPPSERVICE_CLIENTID_DOCKER"
            echo "- AZURE_TENANTID (repository variable)"
            echo "- AZURE_SUBSCRIPTIONID (repository variable)"
            echo "- ACR_LOGIN_SERVER (repository variable)"
            exit 1
          fi
      - name: Login to Azure (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ env.AZURE_CLIENT_ID }}
          tenant-id: ${{ env.AZURE_TENANT_ID }}
          subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy container image to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: Tools-for-the-Tough-Days-SAND
          slot-name: Production
          images: ${{ env.ACR_LOGIN_SERVER }}/${{ env.IMAGE_NAME }}:sand